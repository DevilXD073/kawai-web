import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { Check, Clock3, ArrowLeft, ShieldCheck } from "lucide-react";
import { getProduct, products } from "@/lib/products";
import { formatINR } from "@/lib/utils";

export function generateStaticParams(){return products.map(p=>({slug:p.slug}));}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const p=getProduct(slug);return p?{title:`${p.name} — The Kawai Store`,description:p.description}:{title:"Product — The Kawai Store"}}

export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=getProduct(slug);if(!p)notFound();const discount=p.originalPrice?Math.round((1-p.price/p.originalPrice)*100):0;
return <div className="container-k py-12 md:py-20"><Link href="/shop" className="mb-8 inline-flex items-center gap-2 text-sm text-zinc-500 hover:text-white"><ArrowLeft size={16}/> Back to shop</Link><div className="grid gap-10 lg:grid-cols-[1fr_.9fr]"><div><div className="relative aspect-square overflow-hidden rounded-[2rem] border border-pink-400/15 bg-gradient-to-br from-pink-500/15 via-fuchsia-500/5 to-black"><div className="absolute inset-0" style={{backgroundImage:"radial-gradient(circle at 30% 25%, rgba(255,157,202,.35), transparent 25%), radial-gradient(circle at 70% 70%, rgba(217,38,135,.25), transparent 30%)"}}/><div className="absolute inset-0 grid place-items-center"><div className="rounded-3xl border border-pink-300/20 bg-black/40 px-10 py-8 text-center backdrop-blur"><p className="text-xs tracking-[.3em] text-pink-300">THE KAWAI STORE</p><p className="mt-3 text-xl font-semibold">{p.name}</p></div></div></div></div><div><p className="text-xs font-semibold uppercase tracking-[.2em] text-pink-300">{p.category}</p><h1 className="mt-3 text-4xl font-semibold tracking-tight sm:text-5xl">{p.name}</h1><div className="mt-5 flex items-center gap-3"><span className={`text-sm ${p.available?"text-emerald-300":"text-red-300"}`}>{p.available?"Available":"Out of stock"}</span>{discount>0&&<span className="rounded-full bg-pink-500/10 px-2.5 py-1 text-xs text-pink-200">-{discount}%</span>}</div><p className="mt-6 text-lg leading-8 text-zinc-400">{p.description}</p><div className="mt-7 flex items-end gap-3"><span className="text-3xl font-semibold">{formatINR(p.price)}</span>{p.originalPrice&&<span className="pb-1 text-sm text-zinc-600 line-through">{formatINR(p.originalPrice)}</span>}</div><div className="mt-7 grid gap-3 text-sm text-zinc-400 sm:grid-cols-2"><span className="flex items-center gap-2"><Clock3 size={16} className="text-pink-300"/> {p.deliveryTime}</span><span className="flex items-center gap-2"><ShieldCheck size={16} className="text-pink-300"/> Support ready</span></div><Link href={`/checkout?product=${p.slug}`} className={`mt-8 block rounded-xl px-5 py-4 text-center font-semibold ${p.available?"pink-button":"pointer-events-none bg-white/5 text-zinc-600"}`}>{p.available?"Buy Now":"Out of Stock"}</Link><div className="mt-10 grid gap-7 border-t border-white/5 pt-8 sm:grid-cols-2"><div><h2 className="font-semibold">What You Receive</h2><ul className="mt-3 space-y-2 text-sm text-zinc-500"><li className="flex gap-2"><Check size={15} className="mt-0.5 text-pink-300"/>The agreed digital service</li><li className="flex gap-2"><Check size={15} className="mt-0.5 text-pink-300"/>Delivery within the listed estimate</li></ul></div><div><h2 className="font-semibold">Requirements</h2><ul className="mt-3 space-y-2 text-sm text-zinc-500">{p.requirements.map(x=><li key={x} className="flex gap-2"><Check size={15} className="mt-0.5 text-pink-300"/>{x}</li>)}</ul></div></div></div></div><section className="mt-16 grid gap-5 md:grid-cols-3"><div className="glass rounded-3xl p-6"><h2 className="font-semibold">Delivery Information</h2><p className="mt-3 text-sm leading-6 text-zinc-500">Estimated delivery: {p.deliveryTime}. Timing can vary based on requirements and order volume.</p></div><div className="glass rounded-3xl p-6"><h2 className="font-semibold">Important Information</h2><p className="mt-3 text-sm leading-6 text-zinc-500">Review the requirements before checkout. Custom work may need additional discussion.</p></div><div className="glass rounded-3xl p-6"><h2 className="font-semibold">Reviews</h2><p className="mt-3 text-sm leading-6 text-zinc-500">Genuine reviews will appear here once added by the store owner.</p></div></section></div>}
