import Link from "next/link";
import { ArrowUpRight, MessageCircle, PackageSearch, Sparkles } from "lucide-react";
import SectionHeading from "@/components/SectionHeading";
import { discordUrl } from "@/lib/utils";
const cards=[[MessageCircle,"Discord Support","Join our Discord community.","Join Discord",discordUrl],[PackageSearch,"Order Support","Get assistance with an existing order.","Track Order","/orders/demo"],[Sparkles,"Custom Request","Need something that isn’t listed?","Contact Us","/shop/custom-order"]];
export default function Contact(){return <div className="container-k py-16 md:py-20"><SectionHeading eyebrow="Support" title="Need Help?" subtitle="Our team is here to help you."/><div className="grid gap-5 md:grid-cols-3">{cards.map(([I,title,text,label,href])=>{const Icon=I as typeof MessageCircle;const external=String(href).startsWith("http");return <div key={String(title)} className="glass rounded-3xl p-7"><Icon size={22} className="text-pink-300"/><h2 className="mt-7 font-semibold">{title as string}</h2><p className="mt-2 min-h-12 text-sm leading-6 text-zinc-500">{text as string}</p>{external?<a href={String(href)} target="_blank" rel="noreferrer" className="pink-button mt-5 inline-flex items-center gap-2 rounded-xl px-4 py-2.5 text-sm font-semibold">{label as string}<ArrowUpRight size={15}/></a>:<Link href={String(href)} className="pink-button mt-5 inline-flex items-center gap-2 rounded-xl px-4 py-2.5 text-sm font-semibold">{label as string}<ArrowUpRight size={15}/></Link>}</div>})}</div></div>}
